[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "django-payments-tr"
version = "0.5.1"
description = "Payment processing for Django with Turkey-specific features (iyzico, Stripe, EFT, KDV)"
readme = "README.md"
license = "MIT"
authors = [
    { name = "Emre Aladağ", email = "emre@aladagemre.com" }
]
keywords = [
    "django",
    "payments",
    "turkey",
    "iyzico",
    "stripe",
    "eft",
    "kdv",
    "vat",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Web Environment",
    "Framework :: Django",
    "Framework :: Django :: 4.2",
    "Framework :: Django :: 5.0",
    "Framework :: Django :: 5.1",
    "Framework :: Django :: 5.2",
    "Framework :: Django :: 6.0",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
requires-python = ">=3.12"
dependencies = [
    # Tightened in v0.4.0 to skip vulnerable Django versions surfaced by
    # pip-audit (CVE-2025-13473, CVE-2026-1207, CVE-2026-25674, ...).
    # The marker is conservative — patch releases remain freely available
    # within each supported series.
    "django>=4.2.30,!=5.0.*,!=5.1.*,<7.0",
]

[project.optional-dependencies]
iyzico = ["iyzipay>=1.0"]
stripe = ["stripe>=5.0"]
all = [
    "iyzipay>=1.0",
    "stripe>=5.0",
]
dev = [
    "pytest>=8.0",
    "pytest-django>=4.5",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0",
    "ruff>=0.1.0",
    "mypy>=1.8",
    "django-stubs>=4.2",
    "black>=24.0",
    "isort>=5.0",
    "djangorestframework>=3.14",
    "celery>=5.6.0",
    # Payment providers for testing
    "iyzipay>=1.0",
    "stripe>=5.0",
]

[project.urls]
Homepage = "https://github.com/aladagemre/django-payments-tr"
Documentation = "https://github.com/aladagemre/django-payments-tr#readme"
Repository = "https://github.com/aladagemre/django-payments-tr"
Issues = "https://github.com/aladagemre/django-payments-tr/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/payments_tr"]

[tool.hatch.build.targets.sdist]
exclude = [
    "MIGRATION_PLAN.md",
    "uv.lock",
    ".venv/",
    "__pycache__/",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "settings"
pythonpath = ["src", "tests"]
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]
# Match the CI preview-mode default so local lint catches the same rules
# (e.g. UP042 / StrEnum) that gate the build.
preview = true
# Markdown contains illustrative-only code blocks (CHANGELOG migration
# samples, README usage snippets); preview-mode ruff tries to format
# them, which breaks on intentionally-elided syntax.
extend-exclude = ["*.md", "docs/"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "DJ",     # flake8-django
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "DJ001",  # Allow null=True on CharField for optional EFT fields
]

[tool.black]
line-length = 100
target-version = ["py312"]

[tool.isort]
profile = "black"
line_length = 100
src_paths = ["src", "tests"]

[tool.mypy]
python_version = "3.12"
plugins = ["mypy_django_plugin.main"]
strict = true
warn_unreachable = true

[tool.django-stubs]
django_settings_module = "settings"

[tool.coverage.run]
source = ["src/payments_tr"]
branch = true

[tool.coverage.report]
fail_under = 63
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if typing.TYPE_CHECKING:",
    "@abstractmethod",
]

[dependency-groups]
dev = [
    "bandit>=1.9.2",
    "black>=25.12.0",
    "isort>=7.0.0",
    "pre-commit>=4.5.1",
    "ruff>=0.14.10",
]
